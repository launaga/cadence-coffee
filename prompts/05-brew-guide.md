TASK: Design the Cadence brew guide article page, desktop 1440px.
Article: "Pour-over, 1:16" — a step-by-step method with ratios and timings.

01. HEADER — --surface, 128px top padding.
Columns 2-9: mono uppercase "METHOD / POUR-OVER", H1 serif 3.052rem,
one line of standfirst at 1.25rem in --ink-muted.
Columns 10-12: a mono spec card — RATIO 1:16 / GRIND MEDIUM / TEMP 94°C / TIME 3:30 —
with 1px --line borders and each value in --accent.
Below: full-width 21:9 image of the brew in progress.

02. BODY — single column, columns 3-9, max 65 characters per line.
Must demonstrate every rich-text element styled via tag selectors:
- Paragraphs at 1rem, line height 1.65
- H2 1.953rem and H3 1.563rem, 64px top margin
- Blockquote: 2px --accent left border, 32px padding, 1.25rem, no quote marks, no italic
- Unordered list: a 1px --accent dash instead of a bullet
- Ordered list: mono numerals in --accent — used for the brew steps, each step with
  a mono timestamp ranged right ("0:00", "0:30", "1:15")
- Inline link: --ink with a 1px --accent underline offset 4px
- Figure with a mono uppercase caption beneath
- A data table styled with hairlines and mono values — a grind-size reference chart
- A pull quote: serif 1.953rem spanning columns 2-10, 96px vertical margins

03. TROUBLESHOOTING — --surface-alt. A three-row table: symptom, cause, fix. All hairlines.

04. RELATED GUIDES — 3 austere rows, mono method name, serif title, mono ratio, 1px separators.

05. CTA BAND — to Shop.

FOOTER — identical to homepage.

DELIVERABLE: full article, real brew instructions with real ratios and timings,
every rich-text element present.

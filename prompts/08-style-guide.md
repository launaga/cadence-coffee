TASK: Design the Cadence style guide, desktop 1440px.
Mandatory Webflow submission requirement — must display every styled tag.

01. Header: eyebrow "STYLE GUIDE", H1 "System".
02. COLOUR: swatch grid, 160px squares, 4px radius, token name and hex in mono beneath.
    All seven core tokens plus the four origin hues.
03. TYPOGRAPHY: H1-H6 in sequence, each labelled in mono with its rem size.
    Then paragraph, large, small. Then the three font roles labelled with specimens.
04. TEXT ELEMENTS, each labelled in mono: paragraph · inline link · unordered list ·
    ordered list · blockquote · figure with figcaption · inline code · strong · emphasis ·
    horizontal rule
05. BUTTONS: primary default/hover/focus/disabled, secondary default/hover,
    text link default/hover. Each labelled.
06. FORM ELEMENTS: text input, email, select, textarea, checkbox, radio —
    each in default and focus state, all with visible labels.
07. COMPONENTS: the product card, the tasting chart, the accordion item, the mono eyebrow,
    the data table row — each shown isolated and labelled.
08. SPACING: the scale as horizontal --accent bars, each labelled in mono with px value.

FOOTER — identical to homepage.

DELIVERABLE: complete style guide, every element, every state.

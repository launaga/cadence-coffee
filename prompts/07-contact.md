TASK: Design the Cadence contact page, desktop 1440px.

01. HEADER — --surface, 128px padding. Eyebrow "CONTACT".
H1 serif 3.052rem at column 2: "Talk to the roastery."

02. SPLIT — asymmetric.
Columns 1-5: roastery image bleeding to the left edge, full form height.
Columns 7-12: the form.

FORM SPECIFICATION — this page is graded on form quality:
- Every field has a visible mono uppercase label above it, 11px, --ink-muted.
  Labels, not placeholders.
- Inputs: 1px --line bottom border only, no box, 48px tall.
- Focus state: bottom border becomes 2px --accent, label shifts to --accent. Custom, not default.
- Fields: NAME (text) / EMAIL (email type) / SUBJECT (select) / MESSAGE (textarea, 5 rows)
- SUBJECT options in mono uppercase: GENERAL / ORDER / WHOLESALE / VISIT
- Submit: full width, 56px, --accent fill, mono uppercase.
- Design the success state as a visible variant: a mono confirmation message with a
  1px --accent left border replacing the form. Custom message, not default text.
- Design the error state: 1px --accent bottom border with a mono error line beneath.

03. DIRECT — --surface-alt, three columns with vertical 1px rules:
EMAIL / RESPONSE TIME / ROASTERY HOURS. Mono labels, one line each. No physical address.

FOOTER — identical to homepage.

DELIVERABLE: full contact page including success and error state variants.
